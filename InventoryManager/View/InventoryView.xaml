<UserControl x:Class="InventoryManager.View.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryManager.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="450">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Inventory" FontSize="36" FontFamily="Calibri" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="DarkSlateGray"/>
        <Button Grid.Row="0" Content="Add Item" Width="125" Height="45" HorizontalAlignment="Left" 
                VerticalAlignment="Center" FontSize="24" FontFamily="Calibri" Margin="5,0,0,0"
                Command="{Binding ShowAddItemCommand}"/>
        <Border Grid.Row="0" BorderBrush="DarkSlateGray" BorderThickness="1" />

        <ListBox Grid.Row="1" ItemsSource="{Binding Inventory.Items}" SelectedItem="{Binding CurrentItem}" Height="250" Margin="0,50,0,125">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">

                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="LightBlue"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Trigger>
                    </Style.Triggers>

                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Column="0" IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem},
                              Path=IsSelected}"/>
                        <TextBlock Grid.Column="1" Text="ID" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding Name}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="{Binding Price}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="{Binding Quantity}" VerticalAlignment="Center"/>
                        
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="2" Content="Delete" Command="{Binding Inventory.DeleteItemCommand}" 
                VerticalAlignment="Center" Margin="5,0,0,0" Width="75"/>
        <Button Grid.Row="2" Content="Edit" Command="{Binding ShowEditItemCommand}" 
                VerticalAlignment="Center" Margin="100,0,0,0" Width="75"/>

    </Grid>
</UserControl>
